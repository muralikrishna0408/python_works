teams = ['Data', 'AI', 'DevOps']
for t in teams:
    print('Hello', t, 'Team from Inceptez Technologies')
    print('Keep Learning and Exploring!')

Hello Data Team from Inceptez Technologies
Keep Learning and Exploring!
Hello AI Team from Inceptez Technologies
Keep Learning and Exploring!
Hello DevOps Team from Inceptez Technologies
Keep Learning and Exploring!
------------------------------------------------------------
#scenario#
  temp=input("Enter body temperature: ")
if temp >="25":
    print("it is very hot")
elif temp >= "15" and temp < "25":
    print("it is very mild")
else:
    print("it is very cool")
------------------------------------------------------------
#scenario#
flight = 8000
train = 5000
bus = 2500
if flight <= train and flight <= bus:
    print("I will go by flight")
elif train <= bus and train <= flight:
    print("i will go by train")
else:
    print("i will go by bus")

wallet = 4000
flight = 8000
train = 5000
bus = 2500
if wallet >= flight:
        print("i will go by flight")
elif wallet >= train:
        print("i will go by train")
else:
        print("i will go by bus")

wallet = 4000
flight = 8000
train = 5000
bus = 2500
if wallet >= flight and wallet >= train and wallet >= bus:
        print("i will go by flight")
elif wallet >= train or wallet >= bus or  wallet >= flight:
        print("i will go by train")
else:
        print("i will go by bus")
i will go by train
-------------------------------------------------------------
#scenario#
>>> 2students = 'Ravi'
  File "<python-input-0>", line 1
    2students = 'Ravi'
    ^
SyntaxError: invalid decimal literal
>>> _student_id = 1001
>>> print (_student_id)
1001
>>>
>>> studentName = 'murali'
>>> print (studentName)
murali
>>>
>>> class name = 'python'
  File "<python-input-7>", line 1
    class name = 'python'
               ^
SyntaxError: invalid syntax
>>>
>>> inceptez_batch = 'morning'
>>>
>>> print (inceptez_batch)
morning
--------------------------------------------------
# F Scenario#
age =input("Enter your age: ")
if isinstance(age, str):
    age = int(age)
years_pending = 60 - age
print("Years pending for retirement:", years_pending)
-------------------------------------------------------
# G datatype and casting
employee_name=input("Enter employee name: ")
print ("employee_name", employee_name)
base_salary=float(input("Enter base salary: "))
print("base_salary", base_salary)
hra_percent=int(input("Enter the percent: "))
print("hra_percent", hra_percent)
bonus_amount=float(input("enter bonus amount: "))
print("bonus_amount", bonus_amount)

employee_name Murali
base_salary 250000.0
hra_percent 10
bonus_amount 10000.0
----------------------------------------------
employee_name="Murali"
base_salary=250000.0
hra_percent=10
bonus_amount=10000.0
HRA = base_salary * (hra_percent / 100)
Total_Salary = base_salary + HRA + bonus_amount
print("employee_name", employee_name)
print("base_salary", base_salary)
print("HRA", HRA)
print("bonus_amount", bonus_amount)
print("Total_Salary", Total_Salary)

employee_name Murali
base_salary 250000.0
HRA 25000.0
bonus_amount 10000.0
Total_Salary 285000.0
------------------------------------------------------
marks=input("Enter your mark: ")
try:
    marks=float(marks)
    if marks >= 90:
        print("outstandin")
    elif marks >= 75:
        print("excwllwnt")
    elif marks >= 50:
        print("pass")
    else:
        print("fail")
except ValueError:
    print("Invalid marks. Please enter a number.")

excwllwnt
-----------------------------------------------------------
item_name = input("Enter product name: ")
price = float(input("Enter price per item: "))
quantity = int(input("Enter quantity: "))
total_cost = price * quantity
print("You purchased " , quantity, " units of " , item_name)
print("Total payable: " , total_cost)

You purchased  3  units of  Notepad	
Total payable:  106.5
--------------------------------------------------------------

limit = float(input("Enter total monthly data limit: "))
data_used = int(input("Enter data used: "))
remaining_data = limit - data_used
usage_percentage = data_used / limit * 100
print("your remaining data" , remaining_data, "GB")
print("usage_percentage" , usage_percentage, "GB")
if usage_percentage >= 80:
    print("Warning: High usage, consider upgrading your plan")

your remaining data 5.0 GB
usage_percentage 50.0 GB

--------------------------------------------------------------------
price = float(input("Enter original price: "))
discount_percent = int(input("Enter discount price: "))
discount_amount = price * discount_percent / 100
final_price = int(price - discount_amount)
print("price", price)
print("discount percent", discount_percent)
print("final payable amount", final_price)

price 1000.0
discount percent 28
final payable amount 720
----------------------------------------------------------------------
#bug fixing
age = int(input("Enter age: "))
citizen = input("Are you an Indian citizen? (yes/no)")
if age >= 18 and citizen == "yes":
    print("Eligible to vote")
else:
    print("Not eligible")

Eligible to vote
---------------------------------------------------------------------------
#Banking Eligibility Check
age = int(input("Enter age: "))
monthly_income = int(input("Enter monthly income: "))
if age < 18:
    print("Not eligible for a bank account.")
if age >= 18 and monthly_income < 15000:
    print("Eligible for basic savings account.")
if age >= 18 and monthly_income >= 15000 and monthly_income <= 30000:
    print("Eligible for savings + salary account.")
if age >= 18 and monthly_income > 50000:
    print ("Eligible for premium account")

Eligible for premium account

-------------------------------------------------------------------------








